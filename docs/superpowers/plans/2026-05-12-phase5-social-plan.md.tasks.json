{
  "planPath": "docs/superpowers/plans/2026-05-12-phase5-social-plan.md",
  "tasks": [
    {
      "id": 1,
      "subject": "Task 1: Migration 0008 — schema additions + RLS-ready primitives",
      "status": "completed",
      "completedAt": "2026-05-12",
      "completedSha": "0b7e393",
      "completionNotes": "Two amendments: (1) added missing 0007_pre_phase5_constraints journal entry alongside 0008; (2) removed duplicate reviews_user_published_idx (kept existing reviews_user_published_at_idx from 0005). Carry-forwards documented in plan: T2 must regen 0007+0008 drizzle-kit snapshots (TODO comment at top of migration file); T13 cleans up pre-existing backlog/wishlist logs that were marked with last_event_at by the universal backfill.",
      "description": "**Goal:** Hand-written migration 0008_phase5_social.sql lands all Phase 5 schema changes (3 new tables, 3 new enums, 7 column adds, 9 new indexes, 1 enum extension, 1 dedupe unique index for the notification chokepoint). Applied to live dev DB via Supabase MCP.\n\n**Files:** lib/db/migrations/0008_phase5_social.sql, lib/db/migrations/meta/_journal.json\n\n**Verify:** `pnpm drizzle-kit check` clean + advisor check via MCP\n\n```json:metadata\n{\"files\":[\"lib/db/migrations/0008_phase5_social.sql\",\"lib/db/migrations/meta/_journal.json\"],\"verifyCommand\":\"pnpm drizzle-kit check\",\"acceptanceCriteria\":[\"3 new tables: blocks, list_likes, reports\",\"3 new enum types: report_target_type, report_status, email_digest_cadence\",\"Column adds: lists.slug+published_at, logs.last_event_at+last_event_type, comments.is_hidden, profiles.email_digest_cadence+last_digest_sent_at\",\"9 new indexes including notifications_dedupe_uniq\",\"notification_type enum gains comment_replied\",\"Backfills run for lists.slug and logs.last_event_at\",\"blocks_no_self CHECK enforced\",\"Applied via Supabase MCP\",\"Advisors clean\"]}\n```"
    },
    {
      "id": 2,
      "subject": "Task 2: Drizzle schema.ts mirror",
      "status": "completed",
      "completedAt": "2026-05-12",
      "completedSha": "5ba0f69",
      "completionNotes": "All 9 acceptance criteria PASS. Spec review found no issues. Code review found 0 Critical/Important, 2 Minor: (a) snapshot debt from T1 still open — running `pnpm drizzle-kit generate` would emit a duplicate-work migration since meta/ has snapshots only through 0006; this footgun doesn't bite T3-T29's workflow (none use `generate`, all use `check`); deferred to post-phase cleanup. (b) Typo 'an Index' → 'an index' fixed inline.",
      "blockedBy": [
        1
      ],
      "description": "**Goal:** Update lib/db/schema.ts to mirror migration 0008 changes. Drizzle enums reference existing PG types via pgEnum(). New tables exported as schema.blocks, schema.listLikes, schema.reports.\n\n**Files:** lib/db/schema.ts\n\n**Verify:** `pnpm drizzle-kit check && pnpm typecheck && pnpm lint && pnpm build`\n\n```json:metadata\n{\"files\":[\"lib/db/schema.ts\"],\"verifyCommand\":\"pnpm drizzle-kit check && pnpm typecheck && pnpm lint && pnpm build\",\"acceptanceCriteria\":[\"notificationTypeEnum has 6 values including comment_replied\",\"reportTargetTypeEnum + reportStatusEnum + emailDigestCadenceEnum defined\",\"blocks, listLikes, reports tables exported\",\"profiles has emailDigestCadence + lastDigestSentAt\",\"logs has lastEventAt + lastEventType + userEventIdx partial index\",\"comments has isHidden + reviewCreatedIdx\",\"lists has slug + publishedAt + userSlugUniq + userPublishedIdx\",\"notifications has dedupeUniq\",\"drizzle-kit check reports no drift\"]}\n```"
    },
    {
      "id": 3,
      "subject": "Task 3: Defense-in-depth RLS policies (0003_phase5_prep.sql)",
      "status": "completed",
      "completedAt": "2026-05-12",
      "completedSha": "c3ed013",
      "completionNotes": "RLS policies for blocks/list_likes/reports landed. Self-reviewed (file matches spec byte-for-byte; advisor output confirms relrowsecurity=true on all 3 tables and rls_enabled_no_policy advisor INFO entries are cleared).",
      "blockedBy": [
        1
      ],
      "description": "**Goal:** RLS policies on blocks, list_likes, reports — defense-in-depth same pattern as 0002_phase5_prep.sql from the Codex audit fix.\n\n**Files:** lib/db/policies/0003_phase5_prep.sql\n\n**Verify:** Apply via MCP + advisor check\n\n```json:metadata\n{\"files\":[\"lib/db/policies/0003_phase5_prep.sql\"],\"verifyCommand\":\"Supabase MCP advisor check\",\"acceptanceCriteria\":[\"blocks: own rows only (auth.uid() = blocker_id)\",\"list_likes: SELECT all, INSERT/DELETE own\",\"reports: INSERT auth, SELECT own\",\"RLS enabled on all 3 tables\",\"Applied via MCP\",\"Advisors clean\"]}\n```"
    },
    {
      "id": 4,
      "subject": "Task 4: withBlockedFilter + isBlockedBetween chokepoint",
      "status": "completed",
      "completedAt": "2026-05-12",
      "completedSha": "b72bb53",
      "completionNotes": "Block-filter chokepoint shipped. 133/133 tests pass. Self-reviewed: visibility.ts byte-identical to spec. Implementer's only adjustment was casting `db` to `any` in test mocks (TypeScript saw real Drizzle type which lacks top-level .limit; cast acceptable in mock-structure tests).",
      "blockedBy": [
        2
      ],
      "description": "**Goal:** Block-filter chokepoint that every social read passes through. Forgetting it = leak; Vitest guards.\n\n**Files:** lib/social/_shared/visibility.ts, tests/unit/visibility.test.ts\n\n**Verify:** `pnpm test -- visibility && pnpm typecheck && pnpm lint && pnpm build`\n\n```json:metadata\n{\"files\":[\"lib/social/_shared/visibility.ts\",\"tests/unit/visibility.test.ts\"],\"verifyCommand\":\"pnpm test -- visibility && pnpm typecheck && pnpm lint && pnpm build\",\"acceptanceCriteria\":[\"withBlockedFilter<Q>(viewerId, query, authorIdColumn) returns query w/ bidirectional notExists\",\"viewerId=null short-circuits unchanged\",\"isBlockedBetween(a, b) post-hoc predicate w/ OR clause both directions\",\"Vitest covers: logged-out exception, bidirectional composition, predicate form\"]}\n```"
    },
    {
      "id": 5,
      "subject": "Task 5: getProfileSummary overview hub data shape",
      "status": "completed",
      "completedAt": "2026-05-12",
      "completedSha": "4bdb4c3",
      "completionNotes": "Ships at 0111a92 (impl) + 4bdb4c3 (code-quality follow-ups). Two plan-vs-reality adjustments the implementer made stand: (a) ProfileSummary.recentReviews.rating typed as `string | null` because Drizzle returns `numeric` columns as strings — callers wanting a number do Number(rating); (b) Vitest mock redesigned around a 'thenable chainable' pattern because the 7 queries terminate at three different chain positions (.where for counts, .orderBy for library, .limit for reviews/lists). Code-quality review found 3 Important + 3 Suggestion items all fixed in 4bdb4c3: count() → sql<number>`count(*)::int` matching lib/taste/triggers.ts:51; library no-LIMIT documented; isBlocked:false comment rewritten to cover all 3 branches; dead chainable.limit mock dropped; tierForUser mock removed (real fn is pure); isFollowing=true coverage added (6 test cases total). 139 tests pass; typecheck+lint+build clean.",
      "blockedBy": [
        2,
        4
      ],
      "description": "**Goal:** Single helper loading everything the redesigned /u/[username] page renders — header + stats + taste card + top 3 lists + recent 3 reviews + library truncated. Parallel internal fetches.\n\n**Files:** lib/social/_shared/profile-summary.ts, tests/unit/profile-summary.test.ts\n\n**Verify:** `pnpm test -- profile-summary && pnpm typecheck && pnpm lint && pnpm build`\n\n```json:metadata\n{\"files\":[\"lib/social/_shared/profile-summary.ts\",\"tests/unit/profile-summary.test.ts\"],\"verifyCommand\":\"pnpm test -- profile-summary && pnpm typecheck && pnpm lint && pnpm build\",\"acceptanceCriteria\":[\"getProfileSummary returns ProfileSummary | null\",\"null on not-found, private+non-owner, blocked+non-owner (404 contract)\",\"7 parallel Promise.all fetches\",\"Owner sees own private profile\",\"Vitest covers 5 cases\"]}\n```"
    },
    {
      "id": 6,
      "subject": "Task 6: Follow/unfollow server actions + onFollow notification",
      "status": "completed",
      "completedAt": "2026-05-12",
      "completedSha": "d2c2b73",
      "completionNotes": "Ships at d529f5d (impl) + d2c2b73 (code-quality follow-ups + critical perf fix). Implementer had no plan-vs-reality adjustments — paste-clean. Code-quality reviewer flagged 1 BLOCKING + 2 Important + 1 Suggestion. Blocking item was a real perf gap not introduced by T6 but exposed by it: `follows` table PK leads with follower_id, and Postgres does NOT auto-index FK constraints, so `getFollowers` (WHERE followed_id = X) would table-scan. Fix: migration 0009_follows_followed_id_idx adds `follows_followed_id_idx`, applied to live dev DB via Supabase MCP; schema.ts mirrors. Other fixes: JSDoc on getFollowers/getFollowing matching visibility.ts style; FollowResult comment clarifies unfollow doesn't return self-follow/blocked reasons; viewerId intent comment mirrors profile-summary.ts convention. typecheck+lint+build clean.",
      "blockedBy": [
        4
      ],
      "description": "**Goal:** follow/unfollow/getFollowers/getFollowing server actions. Emit new_follower via stub (T18 ships real emit). Block-filter on follower/following reads.\n\n**Files:** lib/social/follows/server-actions.ts, lib/social/follows/triggers.ts, lib/social/notifications/emit.ts (stub)\n\n**Verify:** `pnpm typecheck && pnpm lint && pnpm build`\n\n```json:metadata\n{\"files\":[\"lib/social/follows/server-actions.ts\",\"lib/social/follows/triggers.ts\",\"lib/social/notifications/emit.ts\"],\"verifyCommand\":\"pnpm typecheck && pnpm lint && pnpm build\",\"acceptanceCriteria\":[\"follow() rejects self-follow + blocked pair + INSERT ON CONFLICT DO NOTHING\",\"unfollow() DELETE idempotent\",\"emit signature locked (drop-in replace in T18)\",\"onFollow emits new_follower on fresh insert only\",\"getFollowers/getFollowing apply withBlockedFilter\"]}\n```"
    },
    {
      "id": 7,
      "subject": "Task 7: Block/unblock + cascade side effects",
      "status": "completed",
      "completedAt": "2026-05-12",
      "completedSha": "f5c589d",
      "completionNotes": "Ships at cbfa239 (impl) + f5c589d (code-quality follow-ups + perf fix + privacy fix). Implementer flagged a plan-prose/plan-code inconsistency: goal text says 're-block re-runs cascade' but code gates cascade on inserted.length > 0; controller accepted gated cascade as correct (conservative; redundant DELETEs would be wasted work). Code-quality reviewer flagged 3 Important + 2 Suggestion items, all fixed in f5c589d: (1) migration 0010 adds comments_user_idx — cascade step (d) was scanning comments unindexed on user_id (mirrors T6's follows.followed_id gap); applied to live dev DB via MCP. (2) block() now revalidates /settings/blocked too. (3) getBlocked() dropped userId parameter — was a latent privacy leak (block lists are private; identity now derived from getCachedUser session, returns [] on logged-out); explicit return type annotation added. (4) cascade-guard comment reframed to lead with idempotency reason. typecheck+lint+build clean throughout.",
      "blockedBy": [
        4,
        6
      ],
      "description": "**Goal:** block/unblock/getBlocked server actions. cascadeBlock strips mutual follows + bidirectional review/list likes + blocked's comments on blocker's reviews.\n\n**Files:** lib/social/blocks/server-actions.ts, lib/social/blocks/side-effects.ts\n\n**Verify:** `pnpm typecheck && pnpm lint && pnpm build`\n\n```json:metadata\n{\"files\":[\"lib/social/blocks/server-actions.ts\",\"lib/social/blocks/side-effects.ts\"],\"verifyCommand\":\"pnpm typecheck && pnpm lint && pnpm build\",\"acceptanceCriteria\":[\"block() rejects self-block + INSERT ON CONFLICT DO NOTHING\",\"cascadeBlock: drops mutual follows + bidirectional review+list likes + blocked's comments\",\"Not transactional; re-block re-runs cascade\",\"unblock() drops block row only\",\"getBlocked returns blocked profile list\"]}\n```"
    },
    {
      "id": 8,
      "subject": "Task 8: Profile overview hub redesign + follower/following routes + follow button",
      "status": "completed",
      "completedAt": "2026-05-12",
      "completedSha": "73ed1d0",
      "completionNotes": "Ships at 0b0c0b7 (impl) + 73ed1d0 (code-quality follow-ups). 10 files, 580+ insertions. Implementer flagged 2 small plan-vs-reality notes: (a) plan said 'pnpm test:e2e -- follow-and-feed' but actual script is 'pnpm e2e' (used real script); (b) implementer added avatarColorFor() fallback for users without avatars mirroring components/ui/avatar.tsx — counts as filling a UX gap, accepted. Code-quality reviewer flagged 1 BLOCKING + 6 Important + 1 Suggestion, all fixed: dialog aria-labelledby via useId, --bg-elevated→--bg-card (token was genuinely undefined; reviewer reality check confirmed), focus return to trigger on dialog close, auth-expired redirect on follow failure, revalidatePath on follow/unfollow (PK lookup → revalidate /u/{username}), Playwright selectors switched to getByLabel/getByRole with 'Log in' (not 'Sign in'), aria-label 'Profile menu'→'More options', dead targetUsername prop removed. Playwright follow test passes in 11.2s; T12 feed assertion is test.fixme placeholder.",
      "blockedBy": [
        5,
        6,
        7
      ],
      "description": "**Goal:** Rebuild /u/[username]/page.tsx into the overview hub. Add /followers + /following routes. Ship FollowButton + BlockAction + ProfileOverviewHeader + FollowersGrid. Playwright covers two-account follow flow.\n\n**Files:** app/(app)/u/[username]/page.tsx, app/(app)/u/[username]/followers/page.tsx, app/(app)/u/[username]/following/page.tsx, components/social/profile-overview-header.tsx, components/social/follow-button.tsx, components/social/block-action.tsx, components/social/followers-grid.tsx, tests/fixtures/test-base.ts, tests/fixtures/seed-test-users.ts, tests/e2e/follow-and-feed.spec.ts\n\n**Verify:** `pnpm typecheck && pnpm lint && pnpm build && pnpm test:e2e -- follow-and-feed`\n\n```json:metadata\n{\"files\":[\"app/(app)/u/[username]/page.tsx\",\"app/(app)/u/[username]/followers/page.tsx\",\"app/(app)/u/[username]/following/page.tsx\",\"components/social/profile-overview-header.tsx\",\"components/social/follow-button.tsx\",\"components/social/block-action.tsx\",\"components/social/followers-grid.tsx\",\"tests/fixtures/test-base.ts\",\"tests/fixtures/seed-test-users.ts\",\"tests/e2e/follow-and-feed.spec.ts\"],\"verifyCommand\":\"pnpm typecheck && pnpm lint && pnpm build && pnpm test:e2e -- follow-and-feed\",\"acceptanceCriteria\":[\"/u/[username] renders 6 sections via getProfileSummary\",\"Each section has See all link\",\"FollowButton optimistic toggle + idempotent rollback\",\"BlockAction overflow + confirm dialog\",\"Two-account Playwright spec passes follow flow\"]}\n```"
    },
    {
      "id": 9,
      "subject": "Task 9: /settings/blocked page + unblock UI",
      "status": "completed",
      "completedAt": "2026-05-13",
      "completedSha": "1f2f668",
      "completionNotes": "Ships at 1f2f668 — small task, no fix loop needed. 2 plan-vs-reality adjustments handled cleanly by implementer: (a) getBlocked() called with no args (T7 fix dropped the userId param); (b) dropdown link uses Radix DropdownMenuItem asChild + Link (plan suggested raw <a>); (c) avatar fallback uses --bg-card token (--bg-elevated is undefined; T8 reality check confirmed). Spec compliant + code-quality approved with one declined suggestion (avatar fallback could mirror Avatar component's initials pattern — borderline value on low-visibility surface, deferred).",
      "blockedBy": [
        7
      ],
      "description": "**Goal:** Settings page showing blocked users + Unblock button per row. Profile dropdown gains link.\n\n**Files:** app/(app)/settings/blocked/page.tsx, components/layout/profile-dropdown.tsx\n\n**Verify:** `pnpm typecheck && pnpm lint && pnpm build`\n\n```json:metadata\n{\"files\":[\"app/(app)/settings/blocked/page.tsx\",\"components/layout/profile-dropdown.tsx\"],\"verifyCommand\":\"pnpm typecheck && pnpm lint && pnpm build\",\"acceptanceCriteria\":[\"/settings/blocked shows blocked list w/ Unblock button\",\"Empty state w/ pixel mascot\",\"Unblock calls server action + revalidates\",\"Profile dropdown link added\"]}\n```"
    },
    {
      "id": 10,
      "subject": "Task 10: buildFeedQuery — pull-on-read UNION ALL across logs + reviews + lists",
      "status": "completed",
      "completedAt": "2026-05-13",
      "completedSha": "4ec9f0e",
      "completionNotes": "Ships at f6a2679 (impl) + 4ec9f0e (code-quality follow-ups). Two implementer adjustments matched codebase: (a) db.execute<T>() returns RowList directly not {rows}; used `unknown as Array<T>` cast matching lib/imports/server-actions.ts pattern; (b) no-await-in-loop eslint rule isn't configured, removed unused disable directive. Code-quality reviewer flagged 2 Important + 2 Suggestion, all fixed: (1) sql.raw UUID interpolation now has safety comment (postgres-js can't bind uuid[] across UNION branches; ::uuid cast rejects non-UUID; IDs from Supabase JWTs); (2) cursors.ts gained `import 'server-only'` (uses Buffer, T11 server action is sole caller — verified no client imports); (3) decodeCursor now guards against invalid Date via Number.isNaN(getTime()); (4) new test pins the invalid-date guard. 9 cursor tests pass.",
      "blockedBy": [
        2,
        4
      ],
      "description": "**Goal:** Feed core query. UNION ALL of 3 projections (logs material events, review publishes, list publishes) from followees, cursor predicate, ORDER BY event_at DESC LIMIT 50. Block filter post-hoc.\n\n**Files:** lib/social/_shared/cursors.ts, lib/social/feed/queries.ts, tests/unit/cursors.test.ts\n\n**Verify:** `pnpm test -- cursors && pnpm typecheck && pnpm lint && pnpm build`\n\n```json:metadata\n{\"files\":[\"lib/social/_shared/cursors.ts\",\"lib/social/feed/queries.ts\",\"tests/unit/cursors.test.ts\"],\"verifyCommand\":\"pnpm test -- cursors && pnpm typecheck && pnpm lint && pnpm build\",\"acceptanceCriteria\":[\"encodeCursor/decodeCursor round-trips eventAt+kind+actorId triplet base64url\",\"compareFeedRows: eventAt DESC then kind ASC then actorId ASC\",\"buildFeedQuery returns FeedRow[] with kind+actorId+eventAt+eventType+targetId+payload\",\"Post-hoc isBlockedBetween filter\",\"Cursor predicate uses (event_at, kind, actor_id) < tuple\"]}\n```"
    },
    {
      "id": 11,
      "subject": "Task 11: getFeed server action + followee lookup",
      "status": "completed",
      "completedAt": "2026-05-13",
      "completedSha": "1884fdd",
      "completionNotes": "Ships at 5ad9cf2 (impl) + 1884fdd (code-quality follow-ups). Single new file lib/social/feed/server-actions.ts. Spec-compliance review passed first pass. Code-quality reviewer flagged 2 Important + 1 Suggestion, all fixed: (1) replaced or(...followeeIds.map(eq)) spread with inArray(blocks.blockedId, followeeIds) and inArray(blocks.blockerId, followeeIds) — emits IN(...) so Postgres uses blocks PK for one arm and migration-0008 reverse index for the other (also avoids JS spread-arg bloat on 500+ followee sets); (2) added JSDoc on getFeed documenting caller-supplied identity contract (NOT session-derived like block()/unblock()/getBlocked() — caller resolves viewer), two-round-trip shape, nextCursor false-positive semantics; (3) renamed blockedSet→blockedFolloweeIds and changed flatMap([r.a,r.b]) to map(r => r.a===viewerId ? r.b : r.a) so the set only contains followee IDs with a block edge (was technically including viewerId — harmless filter-wise but semantically misleading). Re-review approved with regression check clean (or() still used on bidirectional OR; all 4 imports still in active use).",
      "blockedBy": [
        10
      ],
      "description": "**Goal:** Server-action wrapper around buildFeedQuery. Followee IDs fetched once, blocked followees stripped before query, returns { items, nextCursor }.\n\n**Files:** lib/social/feed/server-actions.ts\n\n**Verify:** `pnpm typecheck && pnpm lint && pnpm build`\n\n```json:metadata\n{\"files\":[\"lib/social/feed/server-actions.ts\"],\"verifyCommand\":\"pnpm typecheck && pnpm lint && pnpm build\",\"acceptanceCriteria\":[\"getFeed returns { items, nextCursor }\",\"Empty followees short-circuits to empty result\",\"Blocked followees stripped\",\"nextCursor encoded only when items.length === limit\"]}\n```"
    },
    {
      "id": 12,
      "subject": "Task 12: /home/feed page + feed components + feed-visibility Playwright assertion",
      "status": "completed",
      "completedAt": "2026-05-13",
      "completedSha": "f442601",
      "completionNotes": "Ships at 82aec67 (impl) + efdb3e2 (AC4 fix) + f442601 (code-quality follow-ups). 8 files: app/(app)/home/feed/page.tsx (server component w/ async searchParams Next 16 form, dedup gameIds+actorIds, parallel inArray hydration), components/feed/feed-list.tsx (3-kind branch + Load more cursor link), feed-item-log.tsx (STATUS_VERB map, rating-event differentiation), feed-item-review.tsx (body hook + game cover w/ alt=title), feed-item-list.tsx (title + description), feed-empty-state.tsx (3 modes: no-followees idle pose+CTA, no-events thinking pose+'Quiet around here', no-more terse). nav-tabs.tsx (Feed entry between Home and Library, match p.startsWith('/home/feed')). Playwright spec at tests/e2e/follow-and-feed.spec.ts replaces test.fixme. AC4 gap caught by spec reviewer: getFeed return extended w/ hasFollowees:boolean so page can distinguish 'no followees' from 'followees-but-no-events'. Code-quality fixes: (a) avatar Image unoptimized→raw img w/ eslint-disable matching followers-grid.tsx pattern (would have 400'd at runtime since *.supabase.co not in remotePatterns), (b) feed-item-review cover alt='' → alt={game.title}. Deferred suggestions: typed payload discriminated union (FeedRowLog|FeedRowReview|FeedRowList) for queries.ts, actor-miss comment in feed-list, Playwright scope tightening. Playwright not executed in this session (needs running dev server); spec compiles, typecheck/lint/build clean. FeedRow imports point to lib/social/feed/queries (not server-actions which doesn't re-export).",
      "blockedBy": [
        11,
        13
      ],
      "description": "**Goal:** Wire /home/feed page, 3 item type renderers (log/review/list), empty state w/ mascot, sidebar nav entry, Playwright assertion that follower sees followee review-publish.\n\n**Files:** app/(app)/home/feed/page.tsx, components/feed/feed-list.tsx, components/feed/feed-item-log.tsx, components/feed/feed-item-review.tsx, components/feed/feed-item-list.tsx, components/feed/feed-empty-state.tsx, components/layout/nav-tabs.tsx, tests/e2e/follow-and-feed.spec.ts\n\n**Verify:** `pnpm typecheck && pnpm lint && pnpm build && pnpm test:e2e -- follow-and-feed`\n\n```json:metadata\n{\"files\":[\"app/(app)/home/feed/page.tsx\",\"components/feed/feed-list.tsx\",\"components/feed/feed-item-log.tsx\",\"components/feed/feed-item-review.tsx\",\"components/feed/feed-item-list.tsx\",\"components/feed/feed-empty-state.tsx\",\"components/layout/nav-tabs.tsx\",\"tests/e2e/follow-and-feed.spec.ts\"],\"verifyCommand\":\"pnpm typecheck && pnpm lint && pnpm build && pnpm test:e2e -- follow-and-feed\",\"acceptanceCriteria\":[\"3 item type renderers hydrate game covers + actor avatars\",\"Empty state distinguishes no-followees vs no-more cursor\",\"Sidebar gains Feed entry between Home and Library\",\"Playwright: follower sees followee's review publish\"]}\n```"
    },
    {
      "id": 13,
      "subject": "Task 13: logs.last_event_at population — wire into existing log mutation paths",
      "status": "completed",
      "completedAt": "2026-05-13",
      "completedSha": "89d6ab8",
      "completionNotes": "Ships at f1e3dc8 (impl) + 89d6ab8 (code-quality follow-ups). Files: lib/logs/material-event.ts (NEW — extracted because Turbopack rejects sync exports from 'use server' files; pure module, no directive), lib/logs/server-actions.ts (4 paths wired: createLog, ensureLog playing-branch BOTH values+onConflictDoUpdate, updateLogStatus, updateLogFull — both update paths now SELECT prev row first), lib/imports/merge.ts (insert-branch comment only; merge.ts's update action never touches status/rating so AC8 satisfied vacuously), lib/db/migrations/0011_backfill_cleanup_last_event_at.sql (one-shot cleanup of T1 universal backfill — applied to live DB via Supabase MCP, journal idx 11 added). Tests: 17 cases in last-event-tracking.test.ts (164→165 total). Imports path verified: supabase/functions/_shared/import-engine.ts INSERT only names 6 columns — neither last_event_at nor last_event_type appear, so AC7 satisfied by absence. Code-quality reviewer flagged 1 Important + 3 Suggestions; Important + 1 Suggestion fixed (updateLogStatus + updateLogFull now capture single `const now = new Date()` before .set() and reuse for both updatedAt and lastEventAt — matches ensureLog playingEventAt discipline; added null→playing test case). Deferred suggestions: pgEnum for last_event_type column (future migration), idempotent guard on 0011 (one-shot, won't re-run).",
      "blockedBy": [
        2
      ],
      "description": "**Goal:** Every existing log mutation path sets lastEventAt + lastEventType correctly. Steam/Xbox imports leave NULL. materialEventFromMutation centralizes the rule. Vitest pins truth table.\n\n**Files:** lib/logs/server-actions.ts, lib/imports/merge.ts, tests/unit/last-event-tracking.test.ts\n\n**Verify:** `pnpm test && pnpm typecheck && pnpm lint && pnpm build`\n\n```json:metadata\n{\"files\":[\"lib/logs/server-actions.ts\",\"lib/imports/merge.ts\",\"tests/unit/last-event-tracking.test.ts\"],\"verifyCommand\":\"pnpm test && pnpm typecheck && pnpm lint && pnpm build\",\"acceptanceCriteria\":[\"materialEventFromMutation: status change (not to backlog/wishlist) OR rating set/changed (not cleared)\",\"createLog + updateLog set lastEventAt on material mutations\",\"Imports leave lastEventAt NULL on backlog inserts\",\"Vitest pins 16+ cases\"]}\n```"
    },
    {
      "id": 14,
      "subject": "Task 14: Comments CRUD + mentions parsing",
      "status": "completed",
      "blockedBy": [
        4
      ],
      "description": "**Goal:** createComment/editComment/softDeleteComment server actions + parseMentions + resolveMentionedUserIds + onComment trigger. Comments use T6's emit stub.\n\n**Files:** lib/social/comments/server-actions.ts, lib/social/comments/mentions.ts, lib/social/comments/triggers.ts, tests/unit/mentions.test.ts\n\n**Verify:** `pnpm test -- mentions && pnpm typecheck && pnpm lint && pnpm build`\n\n```json:metadata\n{\"files\":[\"lib/social/comments/server-actions.ts\",\"lib/social/comments/mentions.ts\",\"lib/social/comments/triggers.ts\",\"tests/unit/mentions.test.ts\"],\"verifyCommand\":\"pnpm test -- mentions && pnpm typecheck && pnpm lint && pnpm build\",\"acceptanceCriteria\":[\"createComment: block check, T15-stubbed auto-flag, INSERT, onComment\",\"editComment + softDeleteComment with author-only WHERE\",\"parseMentions strips code fences, handles \\\\@, dedupes, 3-20 char range\",\"onComment emits review_commented OR comment_replied + mention pings\"]}\n```",
      "completionNotes": "T14 shipped at f0a6542 (initial) + 2d35c0c (fixes). 5 new files: lib/social/comments/{server-actions,mentions,triggers}.ts + lib/social/moderation/rules.ts (stub) + tests/unit/mentions.test.ts. createComment auth+block+parent-pre-validation+spam-stub+INSERT+onComment+revalidatePath; editComment+softDeleteComment both author-only WHERE; parseMentions strips code fences and email-local-part prefixes via /(?<![w@\\])@([a-z0-9_]{3,20})(?![a-z0-9_])/gi; onComment branches review_commented/comment_replied with self-notify guards. 10/10 vitest mention cases pass; typecheck/lint/build all clean. emit.ts stub remains a no-op; T22 will land real ON CONFLICT dedupe."
    },
    {
      "id": 15,
      "subject": "Task 15: Spam rules + integration into createComment",
      "status": "completed",
      "blockedBy": [
        14
      ],
      "description": "**Goal:** Replace T14 stub with rule-based checkSpamRules. Pure function <1ms. Vitest pins truth-table boundaries.\n\n**Files:** lib/social/moderation/rules.ts, tests/unit/spam-rules.test.ts\n\n**Verify:** `pnpm test -- spam-rules && pnpm typecheck && pnpm lint && pnpm build`\n\n```json:metadata\n{\"files\":[\"lib/social/moderation/rules.ts\",\"tests/unit/spam-rules.test.ts\"],\"verifyCommand\":\"pnpm test -- spam-rules && pnpm typecheck && pnpm lint && pnpm build\",\"acceptanceCriteria\":[\"4 rules: link_density (3+ URLs), all_caps (30 chars + 70% caps), repeat_chars (7+ same), blocklist substring\",\"Vitest pins boundaries 2/3 URLs, 29/30 chars, 70/71% caps, 6/7 repeats\",\"Realistic harsh review NOT flagged\",\"Composite multi-rule firing\"]}\n```",
      "completionNotes": "T15 shipped at 73fb706 + comment-cleanup followup. Real checkSpamRules replaces T14 stub with 4 rules: link density (>=3 URLs), all-caps (>=30 chars, strictly >70% caps), repeat-chars (7+ same char in a row), blocklist (7-entry Set, case-insensitive substring). 20/20 vitest cases pin all boundaries (29/30 chars, 70/71% caps, 6/7 repeats, 2/3 URLs) + composite multi-rule case + harsh-review-NOT-flagged case + non-letter repeat coverage. Pure function, ~0.03ms on 5000-char body (10x under <1ms target). server-only import. createComment + editComment now route real flagged comments to reports table with status=auto_flagged."
    },
    {
      "id": 16,
      "subject": "Task 16: Reactions (likeReview, unlikeReview, likeList, unlikeList)",
      "status": "completed",
      "blockedBy": [
        4,
        6
      ],
      "description": "**Goal:** 4 reaction mutations. Each fires emit() on fresh insert; dedupe via notifications_dedupe_uniq from 0008. Block check + self-like silently no-op.\n\n**Files:** lib/social/reactions/server-actions.ts\n\n**Verify:** `pnpm typecheck && pnpm lint && pnpm build`\n\n```json:metadata\n{\"files\":[\"lib/social/reactions/server-actions.ts\"],\"verifyCommand\":\"pnpm typecheck && pnpm lint && pnpm build\",\"acceptanceCriteria\":[\"INSERT ON CONFLICT DO NOTHING (idempotent re-like)\",\"emit on fresh insert; collapsed via dedupe index\",\"Self-like + blocked-pair silently no-op\",\"Cannot like unpublished list\"]}\n```",
      "completionNotes": "T16 shipped at 2cbd557 + JSDoc-deepening followup. lib/social/reactions/server-actions.ts has 4 server actions: likeReview/unlikeReview/likeList/unlikeList. INSERT ON CONFLICT DO NOTHING for idempotent re-like; emit() fires only on fresh insert (returning() row count > 0). Self-like + blocked-pair return ok:true silently (no predicate leak); unpublished/private list returns ok:false (config state). Deliberate skip of revalidatePath documented in JSDoc — like counts ride T17 optimistic toggle, not per-action cache thrash. TOCTOU window vs INSERT noted — feed/queries.ts post-hoc filter is source of truth. emit() remains no-op stub until T18."
    },
    {
      "id": 17,
      "subject": "Task 17: Comments UI (thread + card + composer w/ @-mention autocomplete) + reaction buttons on review page",
      "status": "completed",
      "blockedBy": [
        14,
        15,
        16
      ],
      "description": "**Goal:** Indented one-level thread + composer + flagged badge + LikeButton (review+list flavors). Wire into canonical review page. Playwright covers thread lifecycle + auto-flag hide.\n\n**Files:** components/comments/comment-thread.tsx, components/comments/comment-card.tsx, components/comments/comment-composer.tsx, components/comments/flagged-badge.tsx, components/social/like-button.tsx, app/(app)/u/[username]/reviews/[slug]/page.tsx, tests/e2e/comment-thread.spec.ts, tests/e2e/auto-flag.spec.ts\n\n**Verify:** `pnpm test:e2e -- comment-thread auto-flag && pnpm typecheck && pnpm lint && pnpm build`\n\n```json:metadata\n{\"files\":[\"components/comments/comment-thread.tsx\",\"components/comments/comment-card.tsx\",\"components/comments/comment-composer.tsx\",\"components/comments/flagged-badge.tsx\",\"components/social/like-button.tsx\",\"app/(app)/u/[username]/reviews/[slug]/page.tsx\",\"tests/e2e/comment-thread.spec.ts\",\"tests/e2e/auto-flag.spec.ts\"],\"verifyCommand\":\"pnpm test:e2e -- comment-thread auto-flag && pnpm typecheck && pnpm lint && pnpm build\",\"acceptanceCriteria\":[\"CommentThread renders indented one-level + soft-delete preserves structure\",\"Read predicate: is_hidden=false OR viewer=author\",\"LikeButton optimistic toggle review/list\",\"Playwright: comment + reply + edit + soft-delete; auto-flag hidden from non-author\"]}\n```",
      "completionNotes": "T17 shipped at 5daf31c + 02bb985 (review fixes). 4 new comment components (thread/card/composer/flagged-badge), canonical review page mounts CommentThread below ReviewCard via fragment, comments fetch added as 3rd parallel branch in Stage 3. CRITICAL CARRY-FORWARD: unified the duplicate likeReview/unlikeReview that T16 had created — old impl in lib/reviews/server-actions.ts deleted (along with reviewLookupForRevalidate helper), existing components/reviews/like-button.tsx swapped to import from @/lib/social/reactions/server-actions with positional call signature. Real review likes now fire block-check + emit(). 2 Playwright specs: comment-thread (post + reply + edit + soft-delete) + auto-flag (link_density rule, hidden from non-author, visible to author with badge). Final e2e selector for edit/delete scopes by section.filter({hasText:Comments}).getByRole(article).first() — earlier .first() grabbed the composer textarea AND the page-level review article. Review fixes also added revalidateAuthorReviewListing shared helper called from create+edit+softDelete for symmetric cache invalidation. router.refresh() in CommentCard preserved as complementary client-side trigger."
    },
    {
      "id": 18,
      "subject": "Task 18: emit() chokepoint — replace stub with ON CONFLICT dedupe",
      "status": "completed",
      "blockedBy": [
        2,
        4,
        6
      ],
      "description": "**Goal:** Replace T6 stub with real impl: self-notify silence, blocked-pair silence, ON CONFLICT (user_id, type, target_id, actor_id) DO UPDATE bumps created_at + clears read_at.\n\n**Files:** lib/social/notifications/emit.ts, tests/unit/emit-dedupe.test.ts\n\n**Verify:** `pnpm test -- emit-dedupe && pnpm typecheck && pnpm lint && pnpm build`\n\n```json:metadata\n{\"files\":[\"lib/social/notifications/emit.ts\",\"tests/unit/emit-dedupe.test.ts\"],\"verifyCommand\":\"pnpm test -- emit-dedupe && pnpm typecheck && pnpm lint && pnpm build\",\"acceptanceCriteria\":[\"Self-notify silenced (recipient === actor)\",\"Blocked-pair silenced\",\"INSERT ... ON CONFLICT DO UPDATE SET created_at=excluded, read_at=null\",\"Vitest covers 3 paths\",\"All T6-T17 callers unchanged\"]}\n```",
      "completionNotes": "T18 shipped at 576f5d7 + best-effort comment polish. emit() now does real INSERT with ON CONFLICT (user_id, type, target_id, actor_id) DO UPDATE SET created_at=excluded.created_at, read_at=NULL. Self-notify + blocked-pair short-circuit before DB call. Backed by notifications_dedupe_uniq unique index from migration 0008. T14 onComment, T16 reactions, T17 unified LikeButton now actually surface inbox rows. 4 vitest cases pin self-notify + blocked-pair + fresh INSERT + ordering. Mock pattern uses thenable chainable: insert→values→onConflictDoUpdate. Live-DB dedupe-bump verification flagged for T19 Playwright ride-along."
    },
    {
      "id": 19,
      "subject": "Task 19: Inbox server actions + /notifications page + bell badge polling",
      "status": "done",
      "blockedBy": [
        18
      ],
      "description": "**Goal:** getInbox/getUnreadCount/markRead/markAllRead + /notifications page (unread-first, filter chips, mark-all-read) + NotificationBell (60s polling). Playwright: action → row + bell increment.\n\n**Files:** lib/social/notifications/server-actions.ts, app/(app)/notifications/page.tsx, components/notifications/notification-row.tsx, components/notifications/notification-bell.tsx, components/layout/nav-tabs.tsx, tests/e2e/notifications-inbox.spec.ts\n\n**Verify:** `pnpm test:e2e -- notifications-inbox && pnpm typecheck && pnpm lint && pnpm build`\n\n```json:metadata\n{\"files\":[\"lib/social/notifications/server-actions.ts\",\"app/(app)/notifications/page.tsx\",\"components/notifications/notification-row.tsx\",\"components/notifications/notification-bell.tsx\",\"components/layout/nav-tabs.tsx\",\"tests/e2e/notifications-inbox.spec.ts\"],\"verifyCommand\":\"pnpm test:e2e -- notifications-inbox && pnpm typecheck && pnpm lint && pnpm build\",\"acceptanceCriteria\":[\"Inbox: unread-first via (read_at IS NULL) DESC then created_at DESC\",\"Filter chips: all/follows/reactions/comments/wishlist\",\"NotificationBell 60s polling for unread count\",\"Playwright: 1 action → 1 row, mark-all-read → 0\"]}\n```",
      "completionNotes": "All 4 server actions (getInbox/getUnreadCount/markRead/markAllRead) fetch the viewer internally via getCachedUser — no client-supplied userId trust surface. Inbox SQL: (read_at IS NULL) DESC then created_at DESC, backed by notifications_user_unread_idx from migration 0008. NotificationBell uses lucide-react Bell (no emoji per project memory), polls every 60s with silent-fail on error, badge clamped at 99+. Mounted in AppHeader for both desktop and mobile (no nav-tabs.tsx change needed — plan listed it speculatively). TARGET_PATH currently routes new_follower → /u/{actor}, other types fall back to /home/feed (TODO(T26) when discovery routes expose username+slug pairs). Playwright switches users via context.clearCookies (no /api/auth/signout route exists). E2E race: the optimistic Follow→Following flip happens synchronously inside startTransition, the actual INSERT runs async — test waits for both label visible AND button enabled (transition cleared) before clearing cookies. typecheck + lint + build clean; e2e passes in 16s on the second run (first run failed due to the missing pending-wait)."
    },
    {
      "id": 20,
      "subject": "Task 20: buildDigest(userId) — pure payload builder",
      "status": "completed",
      "completedAt": "2026-05-13",
      "completedSha": "a447445",
      "completionNotes": "Ships at f0653fd (impl) + a447445 (code-quality follow-ups). Controller pre-corrected 4 plan bugs in the implementer prompt: (1) comment_replied hydration walked reviewTitleMap with parentCommentId as key — would always miss; corrected to a 4th batch query joining comments→reviews→games on comments.id IN parentCommentIds; (2) wishlist_logged_by_friend used Number(uuid) cast — notifications.targetId is uuid, games.id is integer, NaN always; corrected to grouping-only with 'a game' placeholder (no emit path produces this type yet, hydration deferred to whenever the emit path lands); (3) fragile shared ri/ci/wi index walks replaced by storing targetId on each grouped item and walking the grouped arrays (not raw notes); (4) null check moved AFTER yourWeek query — a user with only own log activity now gets a digest. Spec compliance review clean. Code-quality review found 3 Important + 2 Minor: (a) actorDisplayName missing from notifications select → plumbed through and propagated to newFollowers; (b) no regression test for yourWeek-only-activity → non-null path → added; (c) lastDigestSentAt>cadenceStart branch untested → added smoke test; (d/e) added defense-comment for lastEventAt SQL+in-memory layered filter and excerpt-deferral note for T21. 7/7 digest-builder tests pass (206 total); typecheck+lint+build clean throughout.",
      "blockedBy": [
        2,
        18
      ],
      "description": "**Goal:** Pure function returning DigestPayload | null with 5 sections (newFollowers, reactions, comments, wishlistTriggers, yourWeek). Respects last_digest_sent_at window + cadence.\n\n**Files:** lib/social/notifications/digest.ts, tests/unit/digest-builder.test.ts\n\n**Verify:** `pnpm test -- digest-builder && pnpm typecheck && pnpm lint && pnpm build`\n\n```json:metadata\n{\"files\":[\"lib/social/notifications/digest.ts\",\"tests/unit/digest-builder.test.ts\"],\"verifyCommand\":\"pnpm test -- digest-builder && pnpm typecheck && pnpm lint && pnpm build\",\"acceptanceCriteria\":[\"Returns null on cadence=off OR no items OR profile-not-found\",\"Each section capped at SECTION_CAP (10)\",\"Cadence window: 24h daily / 7d weekly\",\"Batched IN-array hydration of target titles\"]}\n```"
    },
    {
      "id": 21,
      "subject": "Task 21: React Email digest template + unsubscribe JWT helpers",
      "status": "completed",
      "completedAt": "2026-05-13",
      "completedSha": "a6fd438",
      "completionNotes": "Ships at 8774d48 (impl) + a6fd438 (code-quality follow-ups). Controller pre-corrected 2 plan bugs: (1) plan imported non-existent `serverEnv` from lib/env.ts — corrected to requireEnv('UNSUBSCRIBE_SECRET') inside getKey() so the env reads at invocation time, not module load; (2) plan said z.string().min(32) for the secret (required) — corrected to optionalString matching the existing pattern, so build doesn't crash when env isn't set locally. Mascot path corrected too: plan said public/mascot-pixel-24.png (asset doesn't exist); switched to existing /mascot/idle.png with width/height='24' attributes — email clients respect the rendered size regardless of source dimensions. Spec compliance review clean (all 16 numbered requirements). Code-quality review found 0 Critical/1 Important about @react-email/components@1.x deprecated on npm (deferred to follow-up; v2 splits into individual packages but our build resolves fine) + 2 actionable Important + 4 Minor. Actionable fixes applied: (a) toLocaleDateString locale-pinned to en-US explicit format in both HTML and plain-text paths (was implicit locale, server-locale-dependent); (b) new tests/unit/unsubscribe-token.test.ts: 4-case round-trip + tamper + garbage + empty-string coverage via vi.stubEnv pattern matching tests/unit/encryption.test.ts precedent. 210 total tests pass (up from 206). typecheck+lint+build clean throughout (1 pre-existing warning in scripts/verify-phase-3.ts:330). No new mascot asset created.",
      "blockedBy": [
        20
      ],
      "description": "**Goal:** React Email template (DigestEmail), plain-text fallback, signUnsubscribeToken + verifyUnsubscribeToken (jose, no expiry). Add deps resend + @react-email/components + @react-email/render + jose.\n\n**Files:** lib/email/digest-template.tsx, lib/email/unsubscribe-token.ts, lib/env.ts, package.json, public/mascot-pixel-24.png\n\n**Verify:** `pnpm typecheck && pnpm lint && pnpm build`\n\n```json:metadata\n{\"files\":[\"lib/email/digest-template.tsx\",\"lib/email/unsubscribe-token.ts\",\"lib/env.ts\",\"package.json\",\"public/mascot-pixel-24.png\"],\"verifyCommand\":\"pnpm typecheck && pnpm lint && pnpm build\",\"acceptanceCriteria\":[\"DigestEmail renders 5 sections conditionally\",\"renderDigestPlainText for multipart fallback\",\"JWT signed w/ UNSUBSCRIBE_SECRET (HS256, no expiry)\",\"UNSUBSCRIBE_SECRET + RESEND_DIGEST_FROM_ADDRESS in env schema\",\"24px mascot pixel sprite in email header\"]}\n```"
    },
    {
      "id": 22,
      "subject": "Task 22: Digest cron via Vercel API route + /unsubscribe route",
      "status": "completed",
      "completedAt": "2026-05-13",
      "completedSha": "b90e7f1",
      "completionNotes": "Ships at e0492c0 (impl) + b90e7f1 (code-quality follow-ups). MIGRATION NOT YET APPLIED — Supabase MCP apply + Vault secret + Vercel CRON_SECRET env setup all deferred to T29 manual gate where user controls secret values. Controller pre-corrected 5 plan bugs in implementer prompt: (1) serverEnv→requireEnv/env split; (2) APP_URL not added (reused NEXT_PUBLIC_APP_URL clientSchema default); (3) RESEND_API_KEY not double-added (already present); (4) CRON_SECRET as optionalString (not .min(32) required); (5) migration filename 20260513_0001 (not 20260512_0002). Implementer added Mascot to /unsubscribe/invalid (plan omitted; accepted as filling acceptance criterion). Spec compliance review clean (all 20 numbered requirements). Code-quality review found 2 Important + 4 Minor — actionable fixes applied: (a) Migration switched from `app.cron_secret` GUC pattern to `vault.decrypted_secrets WHERE name='phase5_cron_secret'` — matches Phase 3/4 pattern; GUC is blocked on managed Supabase. (b) Resend SDK returns {data, error} and does NOT throw — worker now checks sendResult.error explicitly. (c) sent++ reordered before lastDigestSentAt update so a DB write failure post-send is loud + logged with distinct message rather than silently triggering duplicate send on next cron. (d) Response JSON gained `failed` counter alongside `sent` + `candidates`. (e) /unsubscribe/invalid h1 mt-6 added for visual parity with /unsubscribe/confirmed. typecheck+lint+build clean throughout. /unsubscribe pre-fetch tradeoff documented in route JSDoc — state change is idempotent + reversible via settings, accepted for this stage.",
      "blockedBy": [
        21
      ],
      "description": "**Goal:** Pivot from Deno Edge Function to Vercel API route /api/internal/digest/run (header-secret gated) invoked by pg_cron. Plus /unsubscribe JWT-gated route + confirmed/invalid pages.\n\n**Files:** app/api/internal/digest/run/route.ts, lib/env.ts, supabase/migrations/20260512_0002_phase5_digest_cron.sql, app/unsubscribe/route.ts, app/unsubscribe/confirmed/page.tsx, app/unsubscribe/invalid/page.tsx\n\n**Verify:** `pnpm typecheck && pnpm lint && pnpm build` + manual delivery test\n\n```json:metadata\n{\"files\":[\"app/api/internal/digest/run/route.ts\",\"lib/env.ts\",\"supabase/migrations/20260512_0002_phase5_digest_cron.sql\",\"app/unsubscribe/route.ts\",\"app/unsubscribe/confirmed/page.tsx\",\"app/unsubscribe/invalid/page.tsx\"],\"verifyCommand\":\"pnpm typecheck && pnpm lint && pnpm build + manual delivery\",\"acceptanceCriteria\":[\"/api/internal/digest/run header-secret gated; 404 without\",\"Concurrency=5 worker pool for Resend send\",\"last_digest_sent_at updated post-send\",\"pg_cron POSTs daily 12:00 UTC w/ X-Cron-Secret\",\"/unsubscribe verifies JWT, sets cadence=off\",\"Invalid token routes to /unsubscribe/invalid\"]}\n```"
    },
    {
      "id": 23,
      "subject": "Task 23: Lists CRUD + slug + publish + reorder server actions",
      "status": "completed",
      "completedAt": "2026-05-13",
      "completedSha": "5e22f3e",
      "completionNotes": "Ships at 159febd (impl) + 5e22f3e (code-quality follow-ups). Controller pre-corrected 1 plan bug: revalidatePath used `/u/{user.id}/lists` (UUID) instead of `/u/{username}/lists` (route shape); fixed by fetching profile.username first and revalidating BOTH the index AND the canonical /lists/{slug} detail. Implementer added `vi.mock('@/lib/db', …)` to slug.test.ts to bypass env load chain — matches mentions.test.ts/visibility.test.ts precedent. Spec compliance review clean (all 18 numbered requirements + 12 acceptance criteria PASS). Code-quality review found 0 Critical + 3 Important + 5 Minor: actionable Important items applied: (a) UUID validation on publishList + deleteList (inline z.string().uuid().safeParse — matches the other 5 actions without changing signatures); (b) removeItemFromList DELETE + renumber wrapped in db.transaction so partial failure can't leave non-contiguous positions; (c) updateList final UPDATE adds eq(userId) to WHERE for defense-in-depth against future refactors of the findFirst pre-check. Minor items deferred (createList unique-violation try/catch on race documented + accepted; position-race in addItemToList cosmetic only; slug Unicode-stripping intentional + tested). 8/8 slug tests pass (218 total); typecheck/lint/build clean throughout.",
      "blockedBy": [
        2
      ],
      "description": "**Goal:** createList/updateList/publishList/deleteList/addItem/removeItem/reorder + slugifyTitle + uniqueSlugForUser. Slug regenerated only on title change AND unpublished.\n\n**Files:** lib/social/lists/slug.ts, lib/social/lists/server-actions.ts, lib/social/lists/triggers.ts, tests/unit/slug.test.ts\n\n**Verify:** `pnpm test -- slug && pnpm typecheck && pnpm lint && pnpm build`\n\n```json:metadata\n{\"files\":[\"lib/social/lists/slug.ts\",\"lib/social/lists/server-actions.ts\",\"lib/social/lists/triggers.ts\",\"tests/unit/slug.test.ts\"],\"verifyCommand\":\"pnpm test -- slug && pnpm typecheck && pnpm lint && pnpm build\",\"acceptanceCriteria\":[\"slugifyTitle: alphanumeric+dashes, 60-char cap, 'untitled' fallback\",\"uniqueSlugForUser appends -2 -3 on collision\",\"Published lists keep slug forever\",\"publishList sets published_at once\",\"reorderListItems bulk UPDATE in transaction\",\"removeItem renumbers remaining positions\"]}\n```"
    },
    {
      "id": 24,
      "subject": "Task 24: Lists UI — card, detail, editor (with @dnd-kit), add-to-list modal",
      "status": "completed",
      "completedAt": "2026-05-13",
      "completedSha": "815a453",
      "completionNotes": "Ships at aa730df (impl) + 815a453 (code-quality follow-ups). Controller pre-corrected 5 plan bugs in implementer prompt: (1) phantom CSS tokens --bg-elevated/--bg-elevated-2/--accent-fg replaced with --bg-card + text-white; (2) emoji glyphs ⋮⋮/✕ replaced with lucide GripVertical/X (project memory: no emojis in product UI); (3) raw <img> with eslint-disable for cover URLs (RAWG/Supabase not in remotePatterns); (4) Playwright getByLabel('Email') + getByRole login pattern instead of broken input[name]; (5) drag-reorder e2e deferred to T29 M4 (Playwright dragAndDrop doesn't trigger @dnd-kit pointer sensors). Implementer noted 3 DONE_WITH_CONCERNS: drag-reorder e2e deferred (accepted), AddToListModal mounted below GameDetail instead of inside it (accepted; component doesn't accept list props), or(...listIds.map()) pattern instead of inArray() with TODO (fixed in code-quality follow-up). Spec compliance review clean (all 13 verification points + 5 plan corrections satisfied). Code-quality review found 0 Critical + 5 Important + 4 Minor — all 5 Important fixes applied: (a) inArray() replaces or-chains in lists page (matches established convention); (b) updateList revalidates /u/{username}/lists + canonical detail when publishedAt !== null (was a stale-cache bug for edited published lists); (c) ListEditor onRemove + onDragEnd call router.refresh() on result.ok=false to re-sync from authoritative state (was silent desync); (d) AddToListModal create-then-add: setLists immediately after createList succeeds, surface error message when addItemToList fails on the new list (was orphan-list UX); (e) DndContext gains KeyboardSensor + sortableKeyboardCoordinates so keyboard users can reorder (a11y baseline). 4 minor items deferred (SEED_GAME_ID void in spec cleanup, AddToListModal mount UX polish, save-on-blur debouncing, locale-pinned dates in ListDetail). typecheck+lint+build clean throughout.",
      "blockedBy": [
        23
      ],
      "description": "**Goal:** ListCard + ListDetail + ListEditor (@dnd-kit reorder) + AddToListModal. 4 new routes. Playwright covers full lifecycle.\n\n**Files:** package.json, components/lists/list-card.tsx, components/lists/list-detail.tsx, components/lists/list-editor.tsx, components/lists/add-to-list-modal.tsx, app/(app)/lists/new/page.tsx, app/(app)/lists/[id]/edit/page.tsx, app/(app)/u/[username]/lists/page.tsx, app/(app)/u/[username]/lists/[listSlug]/page.tsx, app/(app)/games/[slug]/page.tsx, tests/e2e/lists-flow.spec.ts\n\n**Verify:** `pnpm test:e2e -- lists-flow && pnpm typecheck && pnpm lint && pnpm build`\n\n```json:metadata\n{\"files\":[\"package.json\",\"components/lists/list-card.tsx\",\"components/lists/list-detail.tsx\",\"components/lists/list-editor.tsx\",\"components/lists/add-to-list-modal.tsx\",\"app/(app)/lists/new/page.tsx\",\"app/(app)/lists/[id]/edit/page.tsx\",\"app/(app)/u/[username]/lists/page.tsx\",\"app/(app)/u/[username]/lists/[listSlug]/page.tsx\",\"app/(app)/games/[slug]/page.tsx\",\"tests/e2e/lists-flow.spec.ts\"],\"verifyCommand\":\"pnpm test:e2e -- lists-flow && pnpm typecheck && pnpm lint && pnpm build\",\"acceptanceCriteria\":[\"ListEditor uses @dnd-kit vertical reorder\",\"AddToListModal mounted on game detail page\",\"4 new routes: /lists/new, /lists/[id]/edit, /u/[name]/lists, /u/[name]/lists/[slug]\",\"Playwright covers full lifecycle\"]}\n```"
    },
    {
      "id": 25,
      "subject": "Task 25: Discovery query helpers — popular games, trending reviews, similar users",
      "status": "completed",
      "completedAt": "2026-05-13",
      "completedSha": "81d5add",
      "completionNotes": "Ships at 17936a0 (impl) + 81d5add (code-quality follow-ups). Controller pre-corrected 1 plan bug: db.execute() returns rows directly, NOT {rows:...} — prescribed `as unknown as Array<...>` cast pattern matching lib/social/feed/queries.ts:125 + T22 precedent. All 4 db.execute callsites across 3 files use the cast cleanly. Implementer flagged one acceptable deviation: removed the eslint-disable-next-line no-await-in-loop comment because the rule isn't enabled (the disable itself would warn); replaced with plain inline comment. Spec compliance review clean (all 12 numbered requirements PASS). Code-quality review found 0 Critical + 3 Important + 4 Minor — all 3 Important fixes applied: (a) getSimilarUsers similarity clamped to Math.max(0, 1 - drift()) — drift can return up to 2 when cosine similarity hits -1 on any vector axis, so raw 1-drift can go negative; floor at 0 for UI sanity, JSDoc range claim corrected; (b) getPopularGames JSDoc corrected to accurately describe how unstable_cache appends runtime args to the key (was misleadingly claiming keyParts carries `limit`); trending-reviews had no equivalent claim so no change there; (c) TODO comment added inside similar-users SQL noting that ORDER BY vectors_generated_at DESC biases toward recently-active users at the 500-row cap — revisit at ~10k user mark. typecheck+lint+build clean throughout.",
      "blockedBy": [
        2,
        4
      ],
      "description": "**Goal:** 3 query helpers powering /discover/*. Popular+Trending cached 30min via unstable_cache; similar-users on-demand cosine via Phase 4 drift().\n\n**Files:** lib/social/discovery/popular-games.ts, lib/social/discovery/trending-reviews.ts, lib/social/discovery/similar-users.ts\n\n**Verify:** `pnpm typecheck && pnpm lint && pnpm build`\n\n```json:metadata\n{\"files\":[\"lib/social/discovery/popular-games.ts\",\"lib/social/discovery/trending-reviews.ts\",\"lib/social/discovery/similar-users.ts\"],\"verifyCommand\":\"pnpm typecheck && pnpm lint && pnpm build\",\"acceptanceCriteria\":[\"getPopularGames: unstable_cache 1800s TTL\",\"getTrendingReviews: cached unfiltered, block-filter post-cache per viewer\",\"getSimilarUsers: empty-tier short-circuit, 500-row candidate pool, top-N by similarity\"]}\n```"
    },
    {
      "id": 26,
      "subject": "Task 26: Discovery routes + components",
      "status": "completed",
      "blockedBy": [
        25
      ],
      "description": "**Goal:** /discover landing + 3 sub-pages + 3 components. Discover entry in sidebar.\n\n**Files:** app/(app)/discover/page.tsx, app/(app)/discover/games/page.tsx, app/(app)/discover/reviews/page.tsx, app/(app)/discover/people/page.tsx, components/discovery/popular-games-grid.tsx, components/discovery/trending-reviews-list.tsx, components/discovery/similar-users-row.tsx, components/layout/nav-tabs.tsx\n\n**Verify:** `pnpm typecheck && pnpm lint && pnpm build`\n\n```json:metadata\n{\"files\":[\"app/(app)/discover/page.tsx\",\"app/(app)/discover/games/page.tsx\",\"app/(app)/discover/reviews/page.tsx\",\"app/(app)/discover/people/page.tsx\",\"components/discovery/popular-games-grid.tsx\",\"components/discovery/trending-reviews-list.tsx\",\"components/discovery/similar-users-row.tsx\",\"components/layout/nav-tabs.tsx\"],\"verifyCommand\":\"pnpm typecheck && pnpm lint && pnpm build\",\"acceptanceCriteria\":[\"/discover landing previews top 4 of each section\",\"3 sub-pages with correct ordering + SEO meta\",\"/discover/people noindex (auth-gated)\",\"Sidebar Discover entry\"]}\n```",
      "completedAt": "2026-05-13",
      "completedSha": "7813b7c",
      "completionNotes": "Discovery routes + components shipped. 4 pages (/discover landing + 3 sub-pages) and 3 server components (PopularGamesGrid, TrendingReviewsList, SimilarUsersRow). NavTabs and MobileMenu both gained Discover entry between Feed/Home and Library. /discover/people is auth-gated (notFound for logged-out) and noindex/nofollow. SimilarUsersRow has focus-within ring on <li> for keyboard a11y (Link.contents hides the global :focus-visible outline). Spec PASS; code-quality APPROVE with 2 minor follow-ups landed at 7813b7c (focus indicator + stale eslint comments)."
    },
    {
      "id": 27,
      "subject": "Task 27: Moderation — reports, admin queue, report modal",
      "status": "completed",
      "blockedBy": [
        15,
        17,
        24
      ],
      "description": "**Goal:** ReportModal on review/comment/list overflow. createReport (rate-limited 10/hr) + resolveReport (admin only). /admin/reports queue env-gated via ADMIN_USER_IDS. Playwright covers env gate + admin resolve.\n\n**Files:** lib/social/moderation/admin.ts, lib/social/moderation/server-actions.ts, components/moderation/report-modal.tsx, components/moderation/reports-queue.tsx, components/moderation/moderation-actions.tsx, app/(app)/admin/reports/page.tsx, app/(app)/u/[username]/reviews/[slug]/page.tsx, app/(app)/u/[username]/lists/[listSlug]/page.tsx, lib/env.ts, tests/e2e/admin-gate.spec.ts\n\n**Verify:** `pnpm test:e2e -- admin-gate && pnpm typecheck && pnpm lint && pnpm build`\n\n```json:metadata\n{\"files\":[\"lib/social/moderation/admin.ts\",\"lib/social/moderation/server-actions.ts\",\"components/moderation/report-modal.tsx\",\"components/moderation/reports-queue.tsx\",\"components/moderation/moderation-actions.tsx\",\"app/(app)/admin/reports/page.tsx\",\"app/(app)/u/[username]/reviews/[slug]/page.tsx\",\"app/(app)/u/[username]/lists/[listSlug]/page.tsx\",\"lib/env.ts\",\"tests/e2e/admin-gate.spec.ts\"],\"verifyCommand\":\"pnpm test:e2e -- admin-gate && pnpm typecheck && pnpm lint && pnpm build\",\"acceptanceCriteria\":[\"createReport rate-limited 10/hr/user\",\"resolveReport: hide → comments.is_hidden=true OR keep → resolved_no_action\",\"isAdmin checks ADMIN_USER_IDS env (comma-separated UUIDs)\",\"/admin/reports notFound() for non-admin\",\"Playwright: non-admin 404\"]}\n```",
      "completedAt": "2026-05-13",
      "completedSha": "bedd81c",
      "completionNotes": "Moderation surface shipped: isAdmin allowlist via env.ADMIN_USER_IDS, createReport (10/hr/user Redis rate limit), resolveReport (admin-only, hide comments / metadata-only for other targets), getReportQueue (own server-only file). ReportModal mounted on review canonical page, comment cards, and list canonical page (logged-in non-owners only). /admin/reports notFound()s non-admins. Playwright admin-gate.spec verifies 404 gate. Plan corrections baked in: env exports env not serverEnv; Radix Dialog used not raw fixed inset-0; real CSS tokens only; lucide icons only; getByLabel login pattern. Code-quality follow-ups landed at bedd81c (DialogTitle on success branch, sonner toast over alert, DialogTrigger asChild). Carry-forwards (Minor): non-comment hide is metadata-only (documented), revalidatePath only hits /admin/reports (acceptable Phase 5 scope), modal form state persists across reopens (intentional)."
    },
    {
      "id": 28,
      "subject": "Task 28: scripts/verify-phase-5.ts — automated verification pass",
      "status": "completed",
      "blockedBy": [
        3,
        5,
        8,
        9,
        12,
        13,
        17,
        19,
        22,
        24,
        26,
        27
      ],
      "description": "**Goal:** Mirror verify-phase-4.ts shape. 10 automated groups matching the verify-gate criteria + 5 manual items printed for operator.\n\n**Files:** scripts/verify-phase-5.ts\n\n**Verify:** `pnpm tsx scripts/verify-phase-5.ts && pnpm typecheck && pnpm lint && pnpm build`\n\n```json:metadata\n{\"files\":[\"scripts/verify-phase-5.ts\"],\"verifyCommand\":\"pnpm tsx scripts/verify-phase-5.ts && pnpm typecheck && pnpm lint && pnpm build\",\"acceptanceCriteria\":[\"10 groups corresponding to verify-gate criteria\",\"Each check has unique ID (G1.1, G1.2, ...)\",\"Exit code 0 only if all PASS or explicit SKIP\",\"M1-M5 manual items printed at end\",\"Runs in <30s\"]}\n```",
      "completedAt": "2026-05-13",
      "completedSha": "f42a52a",
      "completionNotes": "verify-phase-5.ts shipped (625 lines, 10 groups, 29 checks). Mirrors verify-phase-4 shape with postgres-js direct SQL + file-shape regex checks. Runs in ~2s, 29/29 PASS on phase-5-social against live dev DB. Spec PASS; code-quality APPROVE with 2 small Minor hardening fixes landed at f42a52a (G5.1 exact-count enforcement + per-group try/catch via runGroup helper). Deferred Minor carry-forwards (predicate brittleness in G3.3/G4.3/G5.3/G7.x — false-negative risk if files refactor, but no current breakage; tighten if any regression surfaces). M1-M5 manual gate items printed at the end for T29."
    },
    {
      "id": 29,
      "subject": "Task 29: Manual gate verification + final polish + tag phase-5-complete",
      "status": "pending",
      "blockedBy": [
        28
      ],
      "description": "**Goal:** Close the phase. Run M1-M5 manual gate items, polish rough edges, update memory, tag phase-5-complete on main HEAD.\n\n**Files:** memory/MEMORY.md, memory/phase_5_complete.md\n\n**Verify:** `pnpm tsx scripts/verify-phase-5.ts` clean + all manual items signed off in memory\n\n```json:metadata\n{\"files\":[\"memory/MEMORY.md\",\"memory/phase_5_complete.md\"],\"verifyCommand\":\"pnpm tsx scripts/verify-phase-5.ts\",\"acceptanceCriteria\":[\"M1 real digest email delivered + unsubscribe round-trip\",\"M2 visual rendering breakpoints (desktop + mobile)\",\"M3 @-mention autocomplete UX\",\"M4 drag-to-reorder touch + mouse\",\"M5 bell badge polling 2-tab\",\"verify-phase-5.ts runs green\",\"memory updated\",\"git tag phase-5-complete\"]}\n```"
    }
  ],
  "lastUpdated": "2026-05-13T18:23:13.991Z"
}
